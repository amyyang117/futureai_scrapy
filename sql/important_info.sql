CREATE TABLE important_info (
    date DATE NOT NULL,
    name VARCHAR(10) NOT NULL,
    symbol VARCHAR(10) NOT NULL,
    issue_date DATE,
    issue_time time,
    subject VARCHAR,
    a VARCHAR,
    b VARCHAR,
    happen_date DATE,
    content VARCHAR,
    c VARCHAR
)